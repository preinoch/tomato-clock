// format error callback of sequelize's validation
module.export = function(error) {
    switch (error.name) {
        // case: 'SequelizeUniqueConstraintError'
            
    }
    
}

function formatUnique(error) {
    
}